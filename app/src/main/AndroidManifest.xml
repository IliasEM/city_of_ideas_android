<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="treecompany.cityofideas">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:allowBackup="true"
            android:hardwareAccelerated="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:largeHeap="true"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".activities.project.phase.IdeaAddActivity">
        </activity>
        <activity android:name=".activities.project.phase.IdeaActivity">
        </activity>
        <activity android:name=".activities.project.phase.PhaseSurveyActivity">
        </activity>
        <activity android:name=".activities.project.phase.PhaseIdeationActivity">
        </activity>
        <activity android:name=".activities.profile.ProfileActivity">
        </activity>
        <activity android:name=".activities.project.project.ProjectDetailActivity">
        </activity>
        <activity android:name=".activities.platform.PlatformActivity" android:launchMode="singleTop">
        </activity>
        <activity android:name=".activities.qrscanner.QrScannerActivity" android:launchMode="singleTop">
        </activity>
        <activity
                android:name=".activities.profile.AccountActivity">
        </activity>
        <activity android:name=".activities.project.project.ProjectActivity" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>